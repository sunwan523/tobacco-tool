# 📊 烟草价格查询独立应用

这是一个独立的价格查询应用，与主项目共享行情价格数据。

---

## 📁 项目文件说明

| 文件名 | 说明 |
|--------|------|
| `price_query_app.py` | 主应用程序 |
| `start_price_query.bat` | 快速启动脚本 |
| `start_price_query.ps1` | PowerShell 启动脚本 |
| `管理价格查询.bat` | 图形化管理工具（推荐） |
| `manage_price_query.ps1` | PowerShell 管理脚本 |
| `install_price_query_autostart.ps1` | 安装/卸载开机自启动 |
| `Cloudflare Tunnel配置指南.md` | Cloudflare 配置详细指南 |

---

## 🚀 快速启动

### 方式一：使用图形化管理工具（最简单）
双击运行 `管理价格查询.bat`，然后选择"启动应用"

### 方式二：使用批处理脚本
双击运行 `start_price_query.bat` 文件即可

### 方式三：使用命令行
```powershell
streamlit run price_query_app.py --server.port 8502 --server.address 0.0.0.0
```

---

## 🎛️ 使用管理工具

### 启动管理工具
双击 `管理价格查询.bat` 会显示菜单：

```
[1] 启动应用
[2] 停止应用
[3] 重启应用
[4] 查看状态
[5] 安装开机自启动
[6] 卸载开机自启动
[0] 退出
```

### PowerShell 命令行管理
```powershell
# 启动应用
.\manage_price_query.ps1 start

# 停止应用
.\manage_price_query.ps1 stop

# 重启应用
.\manage_price_query.ps1 restart

# 查看状态
.\manage_price_query.ps1 status

# 显示帮助
.\manage_price_query.ps1 help
```

---

## 🔄 开机自启动

### 安装自启动
1. 右键 `install_price_query_autostart.ps1`
2. 选择"以管理员身份运行"
3. 或使用管理工具的选项 5

### 卸载自启动
1. 右键 `install_price_query_autostart.ps1 -Uninstall`
2. 选择"以管理员身份运行"
3. 或使用管理工具的选项 6

### 验证自启动
打开"任务计划程序"，查找名为"烟草价格查询应用_开机自启动"的任务

---

## 🌐 访问地址

启动后可以通过以下地址访问：
- **本地访问**：http://localhost:8502
- **局域网访问**：http://你的IP地址:8502
- **外网访问**：通过 Cloudflare Tunnel 配置

---

## 🔐 Cloudflare Tunnel 配置

### ⚠️ 重要提示
**完全不会影响您的现有 Cloudflare Tunnel 配置！**

### 配置步骤

1. 确保价格查询应用正在运行
2. 登录 https://one.dash.cloudflare.com
3. 进入 **Networks** → **Tunnels**
4. 选择您的 Tunnel
5. 在 **Public Hostname** 中添加：

| 配置项 | 值 |
|--------|-----|
| Subdomain | 您想使用的子域名（如：price） |
| Domain | 您的域名 |
| Service Type | HTTP |
| URL | localhost:8502 |

### 详细指南
请参考 `Cloudflare Tunnel配置指南.md` 文档

---

## 📱 功能特性

### 普通用户功能
- ✅ 快速搜索（商品名称、拼音首字母、条码、盒码）
- ✅ 价格展示（行情价、建议零售价、批发价）
- ✅ 价格库概览统计

### 管理员功能（需要密码）
- ✅ 编辑价格
- ✅ 补录行情价
- ✅ 导出价格表

### 数据安全
- 🔒 管理员密码：`523626`

---

## 📂 数据共享

- ✅ 完全共享 `data/` 目录下的价格数据库
- ✅ 与主项目实时同步数据
- ✅ 任一端修改都会影响另一端

---

## 📊 端口分配

| 应用 | 端口 |
|------|------|
| 主项目（原应用） | 8501 |
| 价格查询应用 | 8502 |
| 您的其他项目 | 8080、8081 等（保持不变） |

---

## 📝 日志文件

应用运行日志保存在：`logs\price_query_app.log`

---

## ⚠️ 注意事项

1. 确保主项目的 `tobacco_core` 目录存在
2. 确保价格数据库文件正常存在
3. 建议在启动前先运行过主项目初始化数据
4. Cloudflare Tunnel 配置需要在 Cloudflare 面板中手动完成
5. 所有现有项目的 Tunnel 配置都不会被影响

---

## 🛠️ 技术说明

- **应用名称**：price_query_app.py
- **默认端口**：8502
- **监听地址**：0.0.0.0（允许外部访问）
- **依赖模块**：tobacco_core.price_store

---

## 🔧 故障排查

### 应用无法启动
```powershell
# 检查端口占用
netstat -ano | findstr ":8502"

# 查看状态
.\manage_price_query.ps1 status
```

### 外网无法访问
1. 确认本地 http://localhost:8502 可以访问
2. 确认 Cloudflare Tunnel 在运行
3. 检查 Public Hostname 配置是否正确

### 查看详细日志
查看 `logs\price_query_app.log` 文件

